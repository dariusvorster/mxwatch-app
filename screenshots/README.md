# Screenshots

Drop PNG files here and reference them from the root README. Recommended set:

- `dashboard.png` — domain health grid + active alerts
- `domain-detail.png` — score ring + pill tabs + Overview tab
- `dmarc.png` — DMARC tab with pass/fail chart and source IP breakdown
- `record-builder.png` — SPF builder with live preview

Capture in light mode at 1440px viewport width for consistency.
